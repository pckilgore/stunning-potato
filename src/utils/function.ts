/* Does nothing. Useful as placeholder for a side effect */
export const noop = () => undefined;

/* Returns what it is passed. Useful as placeholder for a transform */
export const identity = <T>(a: T) => a;
